import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bandb.music',
  appName: 'B and B Music',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
